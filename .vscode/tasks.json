{
    // https://code.visualstudio.com/docs/editor/tasks

    // 任务分组，因为是tasks而不是task，意味着可以连着执行很多任务
    // 在build组的任务们，可以通过在Command Palette(F1) 输入run build task来运行
    // 当然，如果任务分组是test，你就可以用run test task来运行

    "version": "2.0.0",
    "tasks": [
        {
            "label": "make-sort",  
            "type": "shell",  
            "command": "cd ${workspaceFolder}/1-sort && make", 
            "group": { 
                "kind": "build",
                "isDefault": true   
            }
        },
        {
            "label": "make-matrix-multiply", 
            "type": "shell",  
            "command": "cd ${workspaceFolder}/2-matrix-multiply && make",
            "group": { 
                "kind": "build",
                "isDefault": true   
            }
        },
        {
            "label": "make-bfs", 
            "type": "shell",  
            "command": "cd ${workspaceFolder}/3-bfs && make",
            "group": { 
                "kind": "build",
                "isDefault": true   
            }
        },
        {
            "label": "make-triangle-counting", 
            "type": "shell",  
            "command": "cd ${workspaceFolder}/4-triangle-counting && make",
            "group": { 
                "kind": "build",
                "isDefault": true   
            }
        }
    ]
}